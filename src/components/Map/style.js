import styled from "styled-components";

const Mapping = styled.div`
  .leaflet-container {
    display: inherit;
    align-self: center;
    width: 100%;
    height: 100vh;
  }
`;

export default Mapping;
